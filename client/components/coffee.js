import React, { Component } from 'react';

export default class Coffee extends Component {
    static propTypes = {
    };

    render() {
        return (
            <div>Coffee</div>
        );
    }
}
